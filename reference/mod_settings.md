# mod_settings_ui and mod_settings_server

A shiny Module.

## Usage

``` r
mod_settings_ui(id)

mod_settings_server(
  id,
  dataIn = reactive({
     NULL
 }),
  remoteReset = reactive({
     0
 }),
  is.enabled = reactive({
     TRUE
 })
)
```

## Arguments

- id:

  shiny id

- dataIn:

  xxx

## Value

NA

NA
